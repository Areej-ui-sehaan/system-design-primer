---
description: System design skill — interview, design, review, estimate, decide, ood, or study (auto-picks mode)
---

Follow the skill at `skills/system-design/SKILL.md` (package root: `skills/system-design/`).

1. Resolve the mode from the Invocation section using the user request below; if ambiguous, skim `skills/system-design/references/examples.md`.
2. Load only that mode's files (progressive disclosure — do not read the whole package).
3. Run the appropriate flow (4-step interview by default) and close with the checklist/rubric when the mode calls for it.

User request: $ARGUMENTS
